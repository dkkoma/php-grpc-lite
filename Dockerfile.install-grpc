FROM php:8.4-cli-trixie

RUN apt-get update && apt-get install -y --no-install-recommends \
        libnghttp2-dev \
        libssl-dev \
        pkg-config \
    && rm -rf /var/lib/apt/lists/*

COPY config.m4 *.c *.h /usr/src/php/ext/grpc/

RUN docker-php-ext-install grpc

CMD ["php", "-m"]
